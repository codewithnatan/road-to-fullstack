# 🗺️ Road to Fullsttack (My Learning Path & Portfolio)

Welcome to my central learning repository! This is where I documment my structuured jouurney from core JavaScript fundamentals to production-reeady fullstack niche products.

## 🛠️ How I Learn & Build
* **Methood:** Using AI as a mentor (grasping logic and syntax examplees through guidance, never copying pre-made solutions).
* **Focus:** Functtionality and clean logic always come beforeaesthetics.

## 📂 Repository Structure

### 01-javascript-fundamentals/
* **01-todo-list** to **04-number-game**: Core foundations. DOM manipulation, àddEventListener`, `document.getElementById`, state managment and seperation of data.
* **05-text-vault**: Secure notes application focusing on advanced logic and data persistence.
* **06-household-account**: Finance tracker mastering complex state transitions and `.push`/ array manipulation.
* **07-currency-converter**: Math-heavy application handling real-ime input calculations via `.value`.
* **08-weather-app**: First asynchronous REST-API integration using `fetch`, `async` / `await` and `.json()`.
* **09-crypto-ticker**: Independent API project. Fetches live price in Euro. Handled `CORS` and `301` redirects.

### 02-typescript-basics/
* **01-type-declaration**: Introduction to type safety, type annotations like `: string` or `: number` and building scalable `interface`. structures
* **02-secure-functions**: Mastering function parameter validation, return types and the `: void` type using `npx tsc`.